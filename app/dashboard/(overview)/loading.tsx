import DashboardSkeleton from '@/app/ui/skeletons';
// (overview)의 ()경로에는 적용되지 않지만 그룹으로 묶을 때 사용한다.(폴더 그룹)
// dashboard의 로딩화면에만 적용된다.
export default function Loading() {
  // 로딩되기 전까지 보여지는 화면
  // return <DashboardSkeleton />;
}
